# Exodus: шуточные изменения — персональные подписи администрации в Adminwho.
# Для отключения задайте enabled: false и перезапустите сервер.
# Исходный файл копируется при сборке; рабочий файл лежит рядом с серверной программой.
# Редактируется только через файловую систему. Это НЕ CVar и НЕ игровой ресурс.
enabled: true
titles:
  OperatorXeno: "Симплмоб младший"
  # Добавляйте аккаунты отдельными строками с таким же отступом:
  # AnotherAdmin: "Другая шуточная подпись"
