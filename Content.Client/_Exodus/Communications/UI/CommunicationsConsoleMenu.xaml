<controls:FancyWindow xmlns="https://spacestation14.io"
    xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
    Title="{Loc 'comms-console-menu-title'}"
    MinSize="440 400">

    <!-- Main Container -->
    <BoxContainer Orientation="Vertical"
        HorizontalExpand="False"
        VerticalExpand="True"
        Margin="6 6 6 5">

        <TextEdit Name="MessageInput"
            VerticalExpand="True"
            HorizontalExpand="True"
            VerticalAlignment="Stretch"
            HorizontalAlignment="Stretch"
            MinHeight="100"/>

        <PanelContainer Name="WarSection"
            Access="Public"
            StyleClasses="BackgroundDark"
            HorizontalExpand="True"
            Margin="0 4"
            Visible="False">

            <Collapsible BodyVisible="False"
                HorizontalExpand="True"
                Margin="6">

                <CollapsibleHeading Name="WarHeading"
                    Access="Public"
                    Title="{Loc 'war-declaration-console-title'}"
                    HorizontalExpand="True"/>

                <CollapsibleBody>
                    <BoxContainer Orientation="Vertical" HorizontalExpand="True">
                        <Label Name="WarUnlockLabel"
                            Access="Public"
                            HorizontalExpand="True"
                            Margin="0 2"/>

                        <BoxContainer Name="WarRows"
                            Access="Public"
                            Orientation="Vertical"
                            HorizontalExpand="True"
                            SeparationOverride="4"/>
                    </BoxContainer>
                </CollapsibleBody>
            </Collapsible>
        </PanelContainer>

        <!-- ButtonsPart -->
        <BoxContainer Orientation="Vertical"
            VerticalAlignment="Bottom"
            SeparationOverride="4">

            <!-- AnnouncePart -->
            <BoxContainer Orientation="Vertical"
                Margin="0 2">

                <Button Name="AnnounceButton"
                    Access="Public"
                    Text="{Loc 'comms-console-menu-announcement-button'}"
                    ToolTip="{Loc 'comms-console-menu-announcement-button-tooltip'}"
                    StyleClasses="OpenLeft"
                    Margin="0 0 1 0"
                    Disabled="True"/>

                <Button Name="BroadcastButton"
                    Access="Public"
                    Text="{Loc 'comms-console-menu-broadcast-button'}"
                    ToolTip="{Loc 'comms-console-menu-broadcast-button-tooltip'}"
                    StyleClasses="OpenBoth"/>

                <OptionButton Name="AlertLevelButton"
                    Access="Public"
                    ToolTip="{Loc 'comms-console-menu-alert-level-button-tooltip'}"
                    StyleClasses="OpenRight"/>

            </BoxContainer>
        </BoxContainer>
    </BoxContainer>
</controls:FancyWindow>
